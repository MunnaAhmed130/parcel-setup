{
  "name": "percel-setup",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "targets": {
    "main": false
  },
  "browserslist": "> 0.5%, last 2 versions, IE 11",
  "scripts": {
    "clean:output": "rimraf dist",
    "start": "parcel src/pages/*.html",
    "build": "npm run clean:output && parcel build src/pages/*.html",
    "build-no-op": "npm run clean:output && parcel build src/pages/*.html --no-optimize"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.24.0",
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.12.0",
    "postcss-preset-env": "^9.5.1",
    "rimraf": "^5.0.5"
  }
}
